function combinations(options){
    var rtntext;
    switch(options) {
        case '1':
           var html = jQuery('#qOption_'+options).html();
            break;
        default:
            break;
    }
    return rtntext;
}